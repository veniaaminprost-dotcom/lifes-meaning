{
  "name": "lifes-meaning-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm --prefix lifed_maining_platform ci",
    "build": "npm --prefix lifed_maining_platform run build && rm -rf dist && cp -R lifed_maining_platform/dist ./dist",
    "build:landing": "rm -rf dist && mkdir -p dist && cp -R lifes_maining_landing/. ./dist",
    "test": "npm --prefix lifed_maining_platform run test"
  }
}
